name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  windows-build-and-smoke:
    # Project policy: CI must run only on a repository-scoped local self-hosted Windows x64 runner.
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify local toolchain
        shell: pwsh
        run: |
          dotnet --info
          git --version

      - name: Setup Git identity for smoke tests
        shell: pwsh
        run: |
          git config --global user.name "NRS Workbench CI"
          git config --global user.email "nrs-workbench-ci@example.invalid"

      - name: Restore
        run: dotnet restore .\NRSWorkbench.sln

      - name: Build
        run: dotnet build .\NRSWorkbench.sln -c Release --no-restore

      - name: Git safety smoke tests
        run: dotnet run --project .\tests\NRS.Workbench.SmokeTests\NRS.Workbench.SmokeTests.csproj -c Release

      - name: Public source audit
        shell: pwsh
        run: .\scripts\public-audit.ps1
